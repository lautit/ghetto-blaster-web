/** @jsx jsx */
import React from 'react'
import { jsx } from 'theme-ui'
import styled from '@emotion/styled'

const Menu = styled.div`
  h1 {
    font-family: 'Solace for Sadist' !important;
  }
`

export default Menu
